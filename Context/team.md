# Team

> Run `/setup` to fill this in. One person per row; detail lives in `Team/<name>.md`.

| Name | Owns | Key recurring deliverables |
|------|------|----------------------------|
| <name> | <AP / AR / payroll / reporting / tax> | <what they're responsible for, with cadence> |

The assistant uses this to match deliverables to owners in `/team` and `/deadlines`.
