# Data

```
Data/
└── inbox/   Drop raw exports here — accounting reports, bank files, sheets
```

`/report` pulls source data from here. Drop a fresh export in `inbox/` before running a report, then file it under a dated name once used.

**Note:** spreadsheets (`.xlsx`) are git-ignored by default so real company data isn't pushed to a repo. Keep this folder local.
